import ChatPage from ".";

export const metadata = {
  title: "Chat",
  description: "See list of users you chat with",
};

export default async function Account() {

  return (
    <ChatPage />
  )
}